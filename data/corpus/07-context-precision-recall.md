# Context Precision and Recall

Context recall asks whether the relevant supporting facts were retrieved at all. Context precision asks whether the retrieved set contains mostly useful material instead of noise.

A system can have high recall and still be noisy, or high precision and still miss key evidence. Good RAG systems need both.

