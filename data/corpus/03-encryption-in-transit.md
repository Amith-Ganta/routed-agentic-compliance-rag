# Encryption in Transit

Encryption in transit matters anywhere traffic crosses a network boundary, including service-to-service calls and admin access. Encryption in transit uses TLS to protect data as it moves between services so that it cannot be read or altered on the network. Mutual authentication and certificate hygiene keep the channel trustworthy.
