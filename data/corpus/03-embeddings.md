# Embeddings

Embeddings convert text into vectors that capture semantic meaning. Similar questions and passages end up closer together in vector space, which makes dense retrieval possible.

Embedding quality matters because it influences which chunks are surfaced to the generator. Good embeddings can improve recall even before reranking is applied.

