# Agent Evaluation

Agent evaluation checks whether a system can complete a task reliably, not just whether a single answer looks good. The test should cover routing, tool use, grounding, and whether the agent stops when it should.

For bounded agentic systems, evaluation should also confirm that retries and correction loops do not become infinite.

