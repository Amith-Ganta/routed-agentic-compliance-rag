# Vector Databases

Vector databases store embeddings and provide nearest-neighbor search over them. They are useful for RAG because they allow semantic retrieval over large collections of chunks.

Operational details matter too, including persistence, metadata filtering, and how easy it is to rebuild the index when the corpus changes.

