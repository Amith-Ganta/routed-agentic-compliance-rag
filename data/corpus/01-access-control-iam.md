# Access Control and IAM

Access control in cloud governance starts with knowing which identity can assume which role and which actions it may invoke. The principle of least privilege means each identity is granted only the permissions it needs to perform its task and nothing more. In practice that usually means scoped IAM policies, short-lived credentials, and periodic review of access grants.
