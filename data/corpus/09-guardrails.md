# Guardrails

Guardrails are rules that keep a language model inside allowed behavior. They can include content filters, refusal policies, output validation, and routing logic that sends risky queries to safer paths.

In production, guardrails are not only about safety. They also reduce unsupported answers, protect private data, and keep the system easier to reason about.

