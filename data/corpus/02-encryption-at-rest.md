# Encryption at Rest

Encryption at rest is a baseline control for databases, object storage, backups, and block volumes. Encryption at rest protects stored data by keeping it in ciphertext on disk so that a stolen volume or backup cannot be read without the keys. Teams usually pair it with key rotation and tight access to the key management service.
