# Secrets Management

Secrets management reduces the blast radius of leaked credentials by separating secret storage from application code and deployment manifests. Secrets management stores credentials such as API keys and database passwords in a dedicated vault instead of in source code or plain configuration files. Rotation, access policy, and audit trails are part of the same discipline.
